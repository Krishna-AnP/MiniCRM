<h2>Add New Task for <%= client.name %></h2>

<form action="/clients/<%= client.id %>/tasks/new" method="POST" class="form-box">
    <div class="form-group">
        <label>Title</label>
        <input type="text" name="title" required />
    </div>

    <div class="form-group">
        <label>Description</label>
        <textarea name="description"></textarea>
    </div>

    <div class="form-group">
        <label>Status</label>
        <select name="status">
            <option value="pending">Pending</option>
            <option value="completed">Completed</option>
        </select>
    </div>

    <div class="form-group">
        <label>Due Date</label>
        <input type="date" name="due_date" />
    </div>

    <button type="submit">Create Task</button>
</form>

<a href="/clients/<%= client.id %>">⬅ Back to Client</a>

<style>
.form-box {
    max-width: 450px;
    padding: 20px;
    background: #fff;
    border-radius: 6px;
}
.form-group { margin-bottom: 15px; }
label { display: block; font-weight: bold; margin-bottom: 5px; }
input, textarea, select { width: 100%; padding: 8px; }
button { background: #16a34a; color: #fff; padding: 10px; width: 100%; border: none; cursor: pointer; }
</style>
